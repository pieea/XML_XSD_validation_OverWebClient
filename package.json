{
  "name": "fis_assignment1",
  "version": "1.0.0",
  "description": "a simple application to validate xml file using xsd schema file. Using a web client the xml file is submitted to the server that validates the file according t the given schema and states whether the file is valid or not",
  "main": "FirstProject.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1",
    "multer": "^1.3.0",
    "xsd-schema-validator": "^0.5.0"
  }
}
